namespace UISystem.Core.MenuSystem
{
    /// <summary>
    /// Type of stacking for menus.
    /// </summary>
    public enum StackingType
    {
        Add = 0,
        Remove = 1,
        Clear = 2,
        Replace = 3,
    }
}
